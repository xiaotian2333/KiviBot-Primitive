export const pkg = `
{
  "name": "miobot",
  "version": "0.0.0",
  "private": true,
  "author": "Viki <hi@viki.moe> (https://github.com/vikiboss)"
}
`.trim()

export const pkg_js_template = `
{
  "name": "xxx",
  "version": "0.1.0",
  "private": true
}
`.trim()

export const pkg_ts_template = `
{
  "name": "xxx",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc"
  }
}
`.trim()
